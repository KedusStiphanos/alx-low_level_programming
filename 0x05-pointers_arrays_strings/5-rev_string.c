#include "main.h"

/**
 * rev_string - rev string
 * @S: to be reversed
 * Return: String
 */
void rev_string(char *s)
{
	int i, j, k;
j = 0;
{
for (i = 0; s[i] != '\0'; i++)
	j++;
}
{
	for (k = j; k = 0; k = k - 1)
	rev_string(s[j]);
}
return;
}
