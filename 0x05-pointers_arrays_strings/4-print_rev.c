#include "main.h"

/**
 * print_rev - rev string
 * @s: to be reversed
 * Return: String
 */
void print_rev(char *s)
{
	int i, j;

for (i = 0; s[i] != '\0'; i++)
{
	continue;
}
i = i - 1;
for (j = i; j >= 0; j--)
{
		_putchar(s[j]);
}
_putchar('\n');
return;
}
