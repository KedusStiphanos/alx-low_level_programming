#include "main.h"
 /**
 * char *_memcpy() - copies.
 * @dest: to be copied on.
 * @src: to be copied from.
 * @n: the number of copies.
 * Return: Always 0.
 */

char *_memcpy(char *dest, char *src, unsigned int n);
{
  
int i;
for (i = 0; i , i < n, i++)
{    
  dest[i] = src[i];
}
return (dest);
}
